{
    "name": "boxspaced/cms-core-module",
    "require": {

    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "autoload": {
        "psr-4": {
            "Core\\": "src/"
        }
    },
    "config": {
        "bin-dir": "bin/",
        "process-timeout": 1200
    }
}
